spring:
  data:
    mongodb:
      uri: mongodb://localhost:${random.int(10000,65535)}
      embedded:
        version: 4.0.5
  shell:
    interactive:
      enabled: false
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE