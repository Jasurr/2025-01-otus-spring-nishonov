spring:
  data:
    mongodb:
      database: testdb
  shell:
    interactive:
      enabled: false

mongock:
  runner-type: InitializingBean
  enabled: true
  migration-scan-package: ru.otus.hw.migration
logging:
  level:
    org.springframework.data.mongodb: DEBUG
    com.mongodb: DEBUG
    de.flapdoodle.embed: DEBUG